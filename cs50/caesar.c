#include <cs50.h>
#include <ctype.h>
#include <stdio.h>
#include <string.h>

int main(int argc, string argv[])
{
    if(argc == 2)
    {
        const int KEY = atoi(argv[1]);

    }
    else
    {
        printf("Usage: ./caesar key");
        return 1;
    }
}